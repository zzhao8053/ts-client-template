import { styled } from 'styled-components';

export const Finish = styled.div.attrs({
  className:
    'flex justify-center items-center h-full text-center text-gray-500 py-2',
  children: '没有更多了',
})``;
